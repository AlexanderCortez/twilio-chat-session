import Video from 'components/Video';
import { Wrapper } from './styles';

const Participants = () => {
  return (
    <Wrapper>
      <Video bordered autoPlay />
    </Wrapper>
  );
};

export default Participants;
